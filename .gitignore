# dependencies (only needed to run the test suite / bundler locally)
node_modules/

# build + deploy artifacts
dist/
deploy/*.tar.gz

# scratch files
_*.mjs
_*.html

# OS / editor
.DS_Store
*.log
.vercel/
